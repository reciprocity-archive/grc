-content_for :header do
  %h5.highlighted
    =link_to @program.slug, flow_program_path(@program)
    %span.divider /
    Import
  %h1.location Import regulations for #{@program.title}

.row-static
  =render :partial => 'programs/import',
          :layout => 'layouts/widget_box',
          :locals => { :program => @program }
