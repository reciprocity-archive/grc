=form_for @account, :url => flow_account_path(@account), :method => :put, :remote => true do |f|
  =render :partial => 'form',
          :layout  => 'layouts/form_modal',
          :locals  => { :f => f }
