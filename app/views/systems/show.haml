-content_for :header do
  %h5.highlighted
    SYSTEMS
    %span.divider /
    =@system.slug
  %h1.location=@system.title

.row
  .span8
    =render :partial => 'systems/show',
            :layout => 'layouts/widget_box',
            :locals => { :system => @system }

