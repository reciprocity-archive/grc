=form_for :systems, :url => controls_update_flow_system_path(@system), :method => :post, :remote => true do |f|
  =render :partial => 'controls_edit',
          :layout  => 'layouts/list_modal',
          :locals  => { :object => @system, :current => @system.system_controls, :options => Control.all }
