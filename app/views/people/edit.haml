=form_for @person, :url => flow_person_path(@person), :method => :put, :remote => true do |f|
  =render :partial => 'form',
          :layout  => 'layouts/form_modal',
          :locals  => { :f => f }
