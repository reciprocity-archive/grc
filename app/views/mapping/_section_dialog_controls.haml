=form_for section, :url => mapping_update_path(section), :remote => true, :class => 'form-inline' do |f|
  .modal-header
    %a.close{"data-dismiss" => "modal"} x
    %h1 Controls Mapped to #{section.slug}

  .modal-body
    .row-fluid
      .span6.control-group
        %label.cleanlabel Regulation Controls
        %ul.slugtree.nav
          -rcontrols.each do |control|
            %li.row-fluid
              .span12
                .modalSlot
                  %span.prefix
                    =control.slug
                  =control.title

      .span6.control-group
        %label.cleanlabel Company Controls
        %ul.slugtree.nav
          -ccontrols.each do |control|
            %li.row-fluid
              .span12
                .modalSlot
                  %span.prefix
                    =control.slug
                  =control.title

  .modal-footer
    .row-fluid
      %nav.fltlft.txtlft.span3
        =f.submit pat(:cancel), :type => 'reset', :class => 'btn-large btn-info'
      %nav.fltrt.txtrt.span3  
        =f.submit pat(:approve), :class => 'btn-large btn-success'