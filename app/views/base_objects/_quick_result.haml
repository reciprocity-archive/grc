%li
  %a{ :href => self.send("flow_#{controller_name.singularize}_path", item) }
    .item-main
      .item-title
        .row-fluid
          .span12
            .oneline
              %span.code
                #{item.slug}
              %span.title
                #{item.title}
        -#.item-actions
          %a.btn.btn-mini{ :href => self.send("flow_#{controller_name.singularize}_path", item) }
            %i.grcicon-goto
          %a.btn.btn-mini
            %i.grcicon-link