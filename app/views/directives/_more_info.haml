-content_for :widget_title do
  References
  %span=directive.object_documents.count
  , People
  %span=directive.object_people.count

-content_for :widget_help, 'directive_more_info'

-content_for :widget_id, 'directive_more_info_widget'

%ul.tree-structure.simple  
  =render :partial => 'documents/object_list',
          :locals  => { :object => directive }

  =render :partial => 'people/object_list',
          :locals  => { :object => directive }
