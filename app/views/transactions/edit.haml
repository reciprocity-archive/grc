=form_for @transaction, :url => flow_transaction_path(@transaction), :method => :put, :remote => true do |f|
  =render :partial => 'form',
          :layout  => 'layouts/form_modal',
          :locals  => { :f => f }
