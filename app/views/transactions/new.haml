=form_for @transaction, :url => flow_transactions_path, :method => :post, :remote => true do |f|
  =render :partial => 'form',
          :layout  => 'layouts/form_modal',
          :locals  => { :f => f }
