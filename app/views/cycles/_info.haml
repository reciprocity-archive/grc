-content_for :widget_title do
  Audit Info

-content_for :widget_buttons do
  -if has_feature?(:BETA)
    .filter
      .row-fluid
        .span12
          .pull-right
            %a.btn.btn-warning.actionbutton
              Audit Dashboard
            %a.btn.btn-warning.actionbutton
              Invite Auditors

-content_for :widget_help, 'cycle_info'

.row-fluid
  .span6
    %h5 Audit Firm
    %label=cycle.audit_firm
  .span6
    %h5 Audit Lead
    %label=cycle.audit_lead
    


%hr.hr-widget

.row-fluid
  .span12
    =render :partial => 'documents/object_list',
            :locals  => { :object => cycle }

%hr.hr-widget

.row-fluid
  .span12
    =render :partial => 'people/object_list',
            :locals  => { :object => cycle }

%hr.hr-widget

.row-fluid
  .span6.nominheight
    %label.fltlft
      %strong Created:
      #{cycle.created_at.strftime("%m/%d/%y")}
  .span6.nominheight
    %label.fltrt
      %strong Updated:
      #{cycle.updated_at.strftime("%m/%d/%y")}

