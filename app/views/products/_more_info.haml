-content_for :widget_title do
  References
  %span=product.object_documents.count
  , People
  %span=product.object_people.count

-content_for :widget_help, 'product_more_info'

-content_for :widget_id, 'product_more_info_widget'

%ul.tree-structure.simple
  =render :partial => 'documents/object_list',
          :locals  => { :object => product }

  =render :partial => 'people/object_list',
          :locals  => { :object => product }