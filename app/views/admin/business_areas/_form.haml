.group
  =f.label :title
  =f.text_field :title, :class => :text_field

.group.navform.wat-cf
  =f.submit pat(:save), :class => :button
  =f.submit pat(:cancel), :onclick => "window.location='#{business_areas_path}';return false", :class => :button
