.block
  =render :partial => "admin/controls/secnav"
  .content
    %h2.title
      =pat(:new)
      =mt(:control)
    .inner
      =form_for @control, :method => :post, :html => {:class => :form} do |f|
        =render :partial => "admin/controls/form", :locals => { :f => f }

/-content_for :sidebar, partial("base/sidebar")
