  <div class="row-fluid">
    <div class="span12">
      <div class="modalSlot span12">
        <a class="btn btn-blue unmapbtn fltrt" data-type="{{type}}" data-id="{{id}}" href="javascript://">unmap</a>
        <span class="prefix">
          {{prefix}}
          {{slug}}
        </span>
        <br>
        <textarea class="span12" rows="4">{{description}}</textarea>
      </div>
    </div>
  </div>