defaults: &defaults

development:
  database: db/._development.db
  <<: *defaults

  # Add more repositories
  # repositories:
  #   repo1:
  #     adapter:  postgres
  #     database: sample_development
  #     username: the_user
  #     password: secrets
  #     host:     localhost
  #   repo2:
  #     ...

test:
  database: db/._test.db
  adapter: sqlite
  <<: *defaults
production:
  adapter: mysql
  database: cms
  username: cms
  password: cms
  host: localhost
  <<: *defaults
